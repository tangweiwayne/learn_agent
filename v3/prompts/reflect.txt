在结束之前，请做一次自检。**逐条**回答下面 4 个问题，不要跳过，不要展开无关内容：

1. **任务原文要求了什么？** 把原始任务拆成几条具体要求，逐条列出。
   原始任务是：{{task}}

2. **每一条你做到了吗？** 每条给出你已经拿到的证据（哪一步、什么输出）。
   不要为此再跑新命令 —— 用前面已经有的结果。

3. **对照你自己列的计划**：每一条都打勾了吗？
   有没有做【计划之外】的改动？
   如果有，说明为什么必须做；如果不是必须的，现在把它撤销。

4. **结论：能结束吗？**
   - 能 → 执行 `echo {{done_marker}}` 立刻结束，不要再做任何验证。
   - 不能 → 说明还差什么，然后只做那一件事。

<important>
自检只做这一次。上面 4 条回答完，如果结论是"能"，下一步必须直接结束。
不要再"为了保险再跑一遍"。
</important>
